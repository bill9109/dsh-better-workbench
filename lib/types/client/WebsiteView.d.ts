import type { WorkbenchRenderProps } from './types.ts';
/** The webpage owns the full center surface; controls live in its sidebar menu. */
export declare function WebsiteView({ instance }: WorkbenchRenderProps): JSX.Element;
