/**
 * The workbench host owns browser-only DOM roots, so runtime invariant checks
 * are intentionally empty until the host exposes a durable server-side seam.
 */
export declare function apply(): void;
